// 내 예측 입력칸(내픽 / P / 의견)의 선택지 — 상세보기 팝업(MatchDetailModal)과
// 내픽 입력 팝업(MyPickModal) 두 곳이 똑같은 목록을 써야 해서 여기 하나로 모았다.
//
// 예전엔 두 파일에 같은 배열이 각각 있었고, 항목을 하나 추가할 때마다(P-핸↑ 추가,
// P 태그 신설) 두 곳을 손으로 맞춰 고쳐야 했다 — 한쪽만 고치면 팝업마다 선택지가
// 달라지는데 화면에서는 티가 안 나서 알아채기 어렵다.

// 내픽 — 실제로 벳팅한(하려는) 유형. 적중/보험/미적 판정의 기준이 되는 값이다
// (판정 규칙은 columnGroups.js의 PICK_VERDICT_MAP, 서버는 api/main.py의
//  _MY_PICK_VERDICT_MAP — 둘은 같은 값을 내야 한다).
// '2플핸'·'2플핸무'(2026-09-15 추가) — ±2 핸디 기준. 2플핸 = 정배가 2골차 이상 못 이김
// (1골차승·무·역, RT 핸무·무·역), 2플핸무 = 2플핸 적중 + 정배 정확히 2골차승은 보험.
export const PICK_OPTIONS = [
  '대기', '플핸', '플핸무', '플핸승', '2플핸', '2플핸무', '정', '정무', '정역', '핸승', '핸무', '핸승핸무', '무', '역', '무핸무',
]

// 배답픽 — 내픽과 선택지가 완전히 다른, 별도의 참고용 태그(MatchDetailModal.jsx
// '배답픽' 드롭박스 전용). 내픽처럼 판정·집계에는 안 쓰인다(2026-09-12 사용자 지정 —
// 기존 PICK_OPTIONS 재사용을 그만두고 이 전용 목록으로 완전히 교체, 2026-09-20 전면 개편).
export const ODDS_PICK_OPTIONS = [
  '빨/플핸(무)', '빨/플핸(역)', '빨/플핸쪽(무)', '빨/플핸쪽(역)',
  '빨/플핸쪽(무)약', '빨/플핸쪽(역)약', '빨/정배쪽',
  '파/정배(핸승)', '파/정배(핸무)', '파/정배쪽(핸무)', '파/정배쪽(핸승)',
  '파/정배쪽(핸무)약', '파/정배쪽(핸승)약', '파/플핸쪽',
  '엇/정배', '엇/정배쪽', '엇/정배쪽약', '엇/플핸', '엇/플핸쪽', '엇/플핸쪽약',
]

// 상세보기 표본 박스 7개 제목 옆 '방향성' 드롭박스(2026-09-15) — 블루=정배 쪽, 레드=플핸 쪽,
// 엇갈림=방향이 갈림, 몰라=판단 보류. 약블루·약레드(2026-09-15 추가)는 같은 방향이지만 확신이
// 약할 때 쓰는 옅은 단계 — 블루/레드 바로 뒤에 둔다. 표본없음(2026-09-19)은 그 섹션에 볼 표본이
// 없어 방향을 못 정했다는 표시 — 집계 뱃지에는 '표본X'로 나온다. 판정·집계에는 안 쓰인다.
// '크로스'였던 걸 2026-09-20에 '엇갈림'으로 바꿨다 — 판정 칸에 새로 생긴 엇갈림 표시와
// 같은 말을 쓰려는 것(사용자 지정). 기존에 저장된 '크로스' 값은 DB에서 '엇갈림'으로
// 일괄 변경했다(api/my_picks.py migrate_sample_note_direction 참고, 1회성 스크립트로 실행).
// ⚠ 항목을 늘리면 api/main.py SAMPLE_DIRECTIONS에도 같이 넣어야 저장된다.
export const SAMPLE_DIRECTION_OPTIONS = ['블루', '약블루', '레드', '약레드', '엇갈림', '몰라', '표본없음']
// 화면 표기만 바꾼다(2026-09-22 사용자 지정 — 리그 표 국배·해배의 '레드(약)·블루(약)'과 같은 말).
// 저장값('약블루'·'약레드')은 그대로 — 이미 고른 값·서버 검사(SAMPLE_DIRECTIONS)·집계가 전부
// 이 저장값을 쓰므로 바꾸면 안 된다. 여기 없는 값은 저장값을 그대로 보여준다.
export const SAMPLE_DIRECTION_TEXT = { 약블루: '블루(약)', 약레드: '레드(약)' }
export const sampleDirectionText = (v) => SAMPLE_DIRECTION_TEXT[v] ?? v

// 배답벳 — 배답픽과 마찬가지로 내픽과는 완전히 별개인 참고용 태그(MatchDetailModal.jsx
// '배답벳' 드롭박스 전용). 판정·집계에는 안 쓰인다(2026-09-13 추가).
export const ODDS_BET_OPTIONS = ['메인축', '메인벳', '사드축', '사드벳']

// 아카이브 태그 — 팀·맞대결에 붙여 두는 내 판단(2026-09-13 추가). 그 팀/맞대결이 다시
// 나오면 상세보기 경기지표에 📌 뱃지로 뜬다. 판정·집계에는 안 쓰인다. 쓰다가 필요하면
// 여기에 항목만 추가하면 된다(서버는 목록을 따로 검사하지 않는다).
// 팀 태그 — 그 팀이 나오는 모든 경기에 뜬다.
export const TEAM_TAG_OPTIONS = ['최근하락', '최근상승', '홈약세', '원정강세', '감독교체', '부상공백']
// 맞대결 태그 — 방향이 있다(주어 팀 → 상대 팀). '절대상대'=주어 팀이 상대를 못 이긴다,
// '천적'=주어 팀이 상대에게 유독 강하다. 두 팀이 다시 붙을 때만 뜬다.
export const MATCHUP_TAG_OPTIONS = ['절대상대', '천적']
// 배당 태그(2026-09-14 추가) — 팀·맞대결이 아니라 국내 승/무/패 배당 값 자체에 붙인다.
// 같은 값이 나중에 다른 경기(6대리그 아무 리그나)에서 다시 나오면 뜬다. '정배방향'=이
// 배당값에서 정배가 이긴다고 본다, '플핸방향'=플핸(무+역)이 나온다고 본다.
export const ARCHIVE_ODDS_TAG_OPTIONS = ['정배방향', '플핸방향']

// P — 내픽과 별개로 "실제로 딱 찍었는지"만 남기는 참고용 태그. 어떤 집계·판정에도
// 반영되지 않는다(표에서는 RT와 같은 색 뱃지로만 보여준다).
// '2핸승'·'2핸무'·'2플핸' — ±2 핸디 기준 참고용 태그. 순서는 사용자 지정(2026-09-15).
export const P_OPTIONS = ['핸승', '핸무', '무', '역', '2핸승', '2핸무', '2플핸']

// 의견 — 배팅 비중·판단 메모용 태그. 색상은 columnGroups.js의 myHitStyle이 정한다.
// 'P-빅겜' — 빅클럽·더비 등 평소와 다르게 봐야 하는 경기라는 표시.
export const HIT_OPTIONS = [
  'Pass', 'P-빅겜', 'P-고민', 'P-분산', 'P-엇갈', 'P-상대', 'P-상X', 'P-똥배', 'P-똥원', 'P-똥무', 'P-원정', 'P-핸↑', 'P-표X',
  'P-무↑', 'P-역↑', 'P-관전', 'P-어렵',
  'B-고민', 'B-Ma', 'B-Si', '축-플', '축-정', '축-고민', '축-Si',
]

// 결과반성 태그 — 이 픽을 "왜" 이렇게 봤는지, 지표·배당과 다르게 간 근거를 1개만
// 고른다. pick(무엇을 걸지)과는 완전히 별개다 — 판정(적중/보험/미적)에는 전혀
// 반영되지 않는 순수 회고용 기록.
//
// 2026-08-27 기존 메모 55건을 실제로 다 읽고 거기서 쓴 말을 그대로 뽑아 만들었다
// (상상으로 만든 항목이 아니다). "그냥감"은 반드시 있어야 한다 — 이게 없으면
// 모든 픽에 그럴듯한 근거가 붙어서 "근거 있는 판단이 진짜 나은가"를 잴 대조군이
// 사라진다. CLAUDE.md 5-1장(3way 1개 배제 관점)과 같이 쓴다.
// '무고려◯' — 무(RT3)를 염두에 두고 판단한 것이 결과적으로 맞았다는 회고 표시.
// '분석맞음'과 같은 "판단이 맞았다" 계열이라 바로 아래에 둔다.
// 동그라미는 U+25EF(LARGE CIRCLE)를 쓴다 — 흔히 쓰는 ○(U+25CB)는 한글 옆에서
// 너무 작아 보인다. 이 값은 <select> 안에도 그대로 들어가 글자마다 따로 CSS를
// 줄 수 없어서, 스타일이 아니라 더 큰 글자 자체를 골랐다.
// '국배맞음'·'해배맞음' — 국내 지표와 해외 지표가 갈렸을 때(상세보기 방향성 줄의 ≠)
// 어느 쪽을 따랐어야 했는지 남기는 회고 표시. '분석맞음' 계열이라 그 옆에 둔다.
// '분석틀림' — '분석맞음'의 반대. 지표·근거를 따랐는데 결과가 반대로 났다는 회고 표시.
// 짝이 되는 태그라 바로 옆에 둔다.
// '가지말걸'·'갔어야지' — 분석이 맞고 틀리고와 별개로 '들어간 판단' 자체를 되짚는 태그.
// 맞았나/틀렸나를 적는 앞쪽 묶음(분석맞음~해배맞음) 바로 뒤에 붙여 둔다.
// '판정맞음'·'판정틀림' — 상세보기 '시스템 판정'(배당 표 4칸 기반 픽, 2026-09-06 새로
// 설계)이 맞았는지/틀렸는지 남기는 회고 표시. '분석맞음'·'분석틀림'과 같은 짝 구조라
// 그 계열이지만 판단의 상위 결론이라 맨 앞에 둔다.
// '판정보험' — 시스템 판정이 적중도 미적도 아니라 '보험'(적중/보험/미적 3단계 중
// 가운데)으로 난 경우의 회고 표시. '판정맞음'(적중)·'판정틀림'(미적) 사이, 실제
// 판정 순서(적중→보험→미적) 그대로 둔다.
// '조작같아' — 결과가 지표·배당으로는 설명이 안 될 만큼 부자연스러워 승부조작이
// 의심된다는 회고 표시. '사고'(예측 밖의 우발적 변수)와는 다른 의심이라 바로 옆에 둔다.
// '분석분산' — 지표·근거들이 한쪽으로 안 모이고 서로 갈렸다는 회고 표시. 앞자리가 같은
// 태그는 그 계열 바로 아래에 두는 규칙이라(예: 판정맞음/판정틀림), '분석맞음'·'분석틀림'
// 바로 뒤에 붙인다 — '표본분산'(표본 자체가 조건별로 나뉨)과는 다르게 분석 결론이 갈렸다는 뜻.
// '내픽맞음' — 시스템 판정·지표 근거가 아니라 순전히 내 감으로 고른 픽(내픽칸)이 맞았다는
// 회고 표시. '판정맞음'·'분석맞음'과 같은 "맞았다" 계열이지만 근거가 시스템/지표가 아니라
// 나 자신이라 그 계열 맨 뒤(해배맞음 다음)에 둔다.
// '내욕심꽝' — 분석·판정과 무관하게 배당·금액을 욕심껏 키우다 실패했다는 회고 표시.
// '가지말걸'·'갔어야지'(진입 여부를 되짚는 태그)와 비슷하게 "내 판단" 계열이라 그 옆에 둔다.
// '똥배사고' — 똥배(비정상적으로 몰린 배당)를 경계했는데도 '사고'(예측 밖의 우발적
// 변수)로 결과가 갈렸다는 회고 표시. 앞자리가 같은 '똥배경계' 바로 뒤에 둔다.
// '표본맞음' — 지표별 표본(핸승/핸무/무/역 4칸)이 가리킨 방향이 실제로 맞았다는 회고
// 표시. 앞자리가 같은 '표본우세'·'표본분산' 바로 앞에 둔다(맞았다/갈렸다/분산됐다 순,
// 2026-09-14 추가).
// 'RD흐름' — 사용자 지정 태그(2026-09-14 추가). '배당엇갈'·'최근폼'과 같이 시점·시장
// 상황을 근거로 든 계열이라 '최근폼' 바로 뒤에 둔다.
// '쓰나미' — 사용자 지정 태그(2026-09-20 추가). '판정틀림' 바로 아래에 둔다.
export const REASON_TAG_OPTIONS = [
  '판정맞음', '판정보험', '판정틀림', '쓰나미', '분석맞음', '분석틀림', '분석분산', '국배맞음', '해배맞음', '내픽맞음',
  '가지말걸', '갔어야지', '내욕심꽝', '무고려◯', '무체크X',
  '상대전적', '스윕경계', '핸디구조', '표본맞음', '표본우세', '표본분산',
  '배당엇갈', '최근폼', 'RD흐름', '똥배경계', '똥배사고', '시즌초반', '사고', '조작같아', '그냥감',
]
