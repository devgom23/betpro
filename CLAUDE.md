# BETPRO — 클로드 코드 작업 규칙

축구 핸디캡 베팅 분석 시스템. 이 문서는 클로드 코드가 대화 시작 시 자동으로 읽습니다.
(예전 `클로드코드_인수인계.md`를 매번 붙여넣을 필요가 없습니다. 그 문서는 React 전환
이전의 배경 기록이라 히스토리 참고용으로만 두었습니다.)

---

## 0. 소통 규칙

- **한국어로만 소통합니다.** 채팅 답변뿐 아니라 도구 사용 시 표시되는 설명 문구까지 전부
  한국어로 씁니다. 영어·일본어가 섞이면 안 됩니다.
- 사용자는 개발자가 아닙니다. 명령어를 알려줄 때는 **어디에 무엇을 입력하는지**까지 씁니다.
- 코드 수정 후에는 **검증까지 마친 뒤**, 결과를 프로그래밍을 모르는 사람도 이해할 수 있는
  쉬운 말로 설명합니다.
- 작업을 시작할 때 **어떤 모델을 쓰는 게 좋은지 먼저 알려줍니다.** 최소 토큰으로 최대 결과를
  내는 것이 목표입니다.
  - 단순 수정(글자·색상·정렬·간단한 버그) → Sonnet
  - 구조 설계, 엔진 로직, 원인을 모르는 버그, 여러 파일에 걸친 변경 → Opus

### 0-1. 작업을 마쳤을 때의 보고 형식

지시한 일이 끝나면 아래 순서로 정리해서 답합니다. 코드를 못 읽는 사람이 **답변만 보고도
무엇이 어떻게 바뀌었는지, 그렇게 한 근거가 뭔지, 다음에 뭘 하면 되는지** 알 수 있어야 합니다.

1. **요청 항목별로 번호를 붙여 나눈다** — 여러 개를 시켰으면 ① ② ③으로 갈라서, 시킨 것이
   빠짐없이 처리됐는지 사용자가 바로 대조할 수 있게 한다.
2. **바뀐 결과를 눈에 보이는 형태로 보여준다** — 화면 문구·표 모양은 코드가 아니라 실제로
   찍히는 모습 그대로(코드블록이나 표). "이렇게 고쳤습니다"보다 "이렇게 나옵니다"가 낫다.
3. **왜 그렇게 했는지 근거를 숫자로 붙인다** — 실측을 했으면 표본 수와 결과 수치를 표로.
   감으로 정한 것은 감으로 정했다고 밝힌다.
4. **검증한 내용을 밝힌다** — 무엇을 어떻게 확인했는지(브라우저 화면, 린트, 실측 스크립트).
   확인 못 한 부분이 있으면 그것도 말한다.
5. **부수적으로 정리·삭제한 것을 반드시 알린다** — 지운 게 있으면 무엇을 왜 지웠는지,
   되살릴 방법(git 이력)까지. 시킨 것 외에 손댄 것은 숨기지 않는다.
6. **마지막에 다음 행동을 묻는다** — "브라우저에서 확인해 보세요", "커밋할까요?" 등.

지키는 태도:
- **틀렸으면 먼저 인정하고 정정한다.** 앞서 내린 결론이 검증 결과 틀렸으면, 새 결과를
  말하기 전에 그 사실부터 밝힌다.
- **효과가 미미하면 미미하다고 말한다.** 좋아 보이게 포장하지 않는다.
- **표를 적극적으로 쓴다**(6장 관례와 같은 이유 — 나열식 문장은 자릿수가 안 맞아 읽기 힘들다).

---

## 1. 로컬 우선 작업 원칙 (중요)

**작업은 로컬 PC에서 합니다.** 사용자 PC 경로: `C:\Users\gomgu\Desktop\betpro`

1. 파일을 직접 고칩니다. 개발 서버(`npm run dev`)가 켜져 있으면 **저장 즉시 브라우저에 반영**
   되므로, 사용자가 바로 화면에서 확인할 수 있습니다.
2. **커밋과 푸시는 사용자가 요청할 때만 합니다.** 시키지 않았는데 먼저 커밋하지 않습니다.
3. 화면 확인이 필요한 수정은 "고쳤습니다 → 브라우저에서 확인해보세요"까지가 한 묶음입니다.

> 클라우드 세션(Claude Code on the web)에서 작업하면 파일이 GitHub에만 반영되고 로컬 PC에는
> 자동으로 오지 않습니다. 그때는 사용자가 터미널에서 `git pull`을 해야 화면에 보입니다.
> 화면을 바로바로 보면서 작업하려면 **로컬에서 클로드 코드를 실행**해야 합니다.

### 로컬에서 클로드 코드 시작하는 법
명령 프롬프트(cmd)에서:
```
cd /d C:\Users\gomgu\Desktop\betpro
claude
```

---

## 2. 실행 방법

프로그램은 **백엔드 API**와 **프론트엔드 화면** 두 개를 같이 띄워야 동작합니다.

| 대상 | 실행 | 주소 |
|---|---|---|
| 백엔드 (FastAPI) | `API_실행.bat` 더블클릭 | http://localhost:8000 (문서: `/docs`) |
| 프론트 (React/Vite) | `cd web` 후 `npm run dev` | http://localhost:5173 |

로그인: 아이디 `admin` / 비밀번호 `betpro-admin-2026`

---

## 3. 현재 구조

Streamlit 버전은 **완전히 삭제되었고**, 지금은 React + FastAPI 구조입니다.

```
[React 프론트]  ←── HTTP API ──→  [Python 백엔드]
   web/                              api/
```

### 백엔드 `api/`
| 파일 | 역할 |
|---|---|
| `main.py` | FastAPI 진입점, 모든 라우트 |
| `engine.py` | **분석 엔진 (핵심 자산 — 아래 4번 참고)** |
| `standings.py` | 순위·폼·최근전적(HR10/AR10) 계산 |
| `ev_model.py` | 핸승 위험도(RISK/GRADE/ENGINE/WARN/VERDICT) |
| `bet_slips.py` | 베팅내역(조합베팅) |
| `my_picks.py` | 내픽·메모·별표 |
| `data_access.py`, `deps.py` | DB 접근, 의존성 |
| `crawler.py` | 해외배당 수집(스코어맨 — 크롬 창을 띄워 긁는 방식) |
| `kr_crawler.py` | 국내배당·경기결과 수집(와이즈토토 — 로그인·브라우저 불필요) |
| `match_excel.py` | 엑셀 업로드/다운로드 |
| `user_leagues.py` | 사용자 리그 관리 |

루트의 `betpro_auth.py`(로그인), `betpro_paths.py`(4-DB 경로/백업)는 그대로 유지.

### 프론트 `web/src/`
- `pages/` — 화면 단위 (메인, 리그, 상대전적, 통합DB, 베팅내역, 이번주픽, 관리자)
- `components/` — 표·모달·배지 등 재사용 부품
- `context/` — 로그인/테마/글자크기 전역 상태
- `utils/format.js`, `utils/query.js` — 공용 유틸
- `utils/pickOptions.js` — 내픽/P/의견 선택지. **상세보기 팝업과 내픽 팝업이 같이 쓰므로
  항목을 늘릴 때 여기만 고칩니다**(예전엔 두 파일에 같은 배열이 따로 있어 한쪽만 고치면
  팝업마다 선택지가 달라졌습니다)
- `api/client.js` — 백엔드 호출 래퍼

### 4-DB 아키텍처 (그대로 유지할 것)
| DB | 경로 | 용도 |
|---|---|---|
| `auth.db` | 루트 | 계정 정보 |
| `master.db` | `data/master/` | 공식 데이터 (관리자만 쓰기) |
| `user.db` | `data/users/{id}/` | 개인 업로드 데이터 (본인만) |
| `access_log.db` | `data/` | 관리자 열람 기록 |

**원칙**: 공식 데이터와 개인 데이터는 DB 파일 자체가 분리되어 완전 격리됩니다.

### 데이터 캐시 구조 (v2.0) — 조회 성능의 핵심

`api/data_access.py`의 `_CACHE`가 **`(항목이름, DB경로) → (DB파일 mtime, 값)`** 하나로
전부를 관리합니다. DB 파일이 바뀌면 mtime이 달라져 자동으로 무효화되므로, 저장 후
낡은 값이 남는 일은 없습니다.

| 로더 | 내용 | 쓰는 곳 |
|---|---|---|
| `load_league_df` | 리그 원본 그대로 | **DB에 다시 쓰는 경로(업로드/삭제)는 반드시 이것** |
| `load_league_df_ranked` | + 순위(HP/AP)·폼·똥배 | 캐시 안 함. `_ev`의 중간 단계로만 |
| `load_league_df_ev` | + EV·핸승위험도 | **화면·엑셀은 전부 이것** |
| `load_total_df` | 6대리그 합본 | 상대전적(상세보기), 통합DB 탭 |
| `cached_derive(db, 이름, 만드는함수)` | 표가 아닌 파생값(인덱스 등)도 같은 캐시에 | 베팅내역 RT 인덱스, 별표 키 인덱스 |

**지킬 것**
- 리그 표가 필요하면 `load_league_df_ev`를 쓴다. 직접 `attach_for_league`를 부르지 않는다
  (그러면 리그당 80~110ms를 매 요청 반복하게 된다 — v2.0 이전이 그랬다).
- 새로 만든 리그 표를 다른 리그와 합칠 때 `_read_table`을 직접 부르지 않는다.
  반드시 `load_league_df`를 거친다(v2.0 이전 `load_total_df`가 캐시를 우회해
  콜드 2,345ms → 고친 뒤 69ms).
- 요청마다 리그 전체를 훑어 만드는 표(키→행번호 등)가 생기면 지역 변수에 두지 말고
  `cached_derive`에 얹는다.
- **캐시된 df는 절대 수정하지 않는다.** 조회·필터·직렬화만 한다(수정하면 다음 요청이
  오염된 값을 받는다). 컬럼을 붙여야 하면 `.copy()` 먼저.

> 성능을 건드린 뒤에는 "빨라졌다"로 끝내지 말고 **값이 그대로인지** 반드시 대조합니다
> (캐시본 vs 즉석계산본을 `pd.testing.assert_frame_equal`로, 엑셀은 셀 값으로 —
> xlsx는 같은 코드로 뽑아도 바이트가 매번 달라 파일 비교로는 판정할 수 없습니다).

---

## 4. 절대 건드리면 안 되는 것

`api/engine.py`의 분석 함수들 — `preprocess_data()`, `_prep_db()`, `get_samples_fast()`,
`analyze_row()`, `analyze_dataframe()`, `compute_plushandi()`, `_prep_domestic_cache()`.

계산 로직과 numpy 캐싱 최적화가 **오랜 실측 검증을 거쳐 확정된 부분**입니다. 성능과 정확성
양쪽에서 위험하므로, 반드시 필요한 경우 사용자에게 먼저 확인받고 수정합니다.

### 계산 시점 원칙
- 26개 지표와 플핸 예측은 **엑셀 업로드(저장) 시점에 단 한 번만** 계산되어 DB에 저장됩니다.
- 화면에서 조회 조건을 바꿔도 **저장된 값을 불러오기만** 하고 절대 재계산하지 않습니다.
- 예외는 통합DB 탭의 재계산 버튼 2종뿐입니다.

---

## 5. 도메인 용어

- **RT(경기 결과)**: 1=핸승, 2=핸무, 3=무, 4=역
- **핸승**: 정배(이길 것으로 예상된 팀)가 핸디캡을 커버한 경우
- **플핸(플러스 핸디)**: 무+역(RT 3+4)만. 언더독 쪽이 실제로 이기거나 스코어가 비긴 경우
- **플핸무**: 핸무+무+역(RT 2+3+4). 플핸 + 핸디 적중(보험)까지 합친 값 — "핸승이 아닌 나머지
  전부"는 이쪽이다. `api/ev_model.py`·`columnGroups.js`의 확립된 명명 규칙(정승=RT1+2 /
  플핸무=RT2+3+4 / 플핸=RT3+4)을 따른다
- **fav(정배배당)**: `min(FW, FL)`. 배당이 낮은 쪽 = 시장이 강하다고 본 쪽

**배당 컬럼**
- 해외: `FW`(승) `FD`(무) `FL`(패) `FH`(핸디기준점) `FHW` `FHD` `FHL`
- 국내: `KW` `KD` `KL` `KH` `KHW` `KHD` `KHL`
- 경기정보: `L`(리그) `S`(시즌) `R`(라운드) `No` `DT`(일자) `TM`(시간) `HT`(홈팀) `HS`(홈득점)
  `AT`(원정팀) `AS`(원정득점) `RT`(결과)

**26개 지표 코드 규칙**: `[T]{F|K}-{결과}[-{기준}]`
T=6대리그 통합 / F=해외배당 / K=국내배당, 결과=W·L·WL·WDL, 기준=HW·HT·AT

**플핸 예측 5칸** (`compute_plushandi()`): `PH_F` `PH_K` `PH_PICK` `PH_HIT` `PH_DOM`
- 비핸승 중 '핸무'가 최다일 때 적중률이 62.2%로 급락 → PICK에 괄호 표기 + 주황색 경고
- 해)플핸 50~60% 구간은 정보가 없어 관망(—)

---

## 6. 화면 관련 관례

- **대형 분석표**: 2줄 헤더(그룹명 colspan + 세부항목), 컬럼 130개 이상, 행 수백~수천 개.
  성능에 민감하므로 렌더링 방식을 바꿀 때 주의합니다.
- **최근10경기 전적표**: 홈팀은 왼쪽 끝부터, 원정팀은 오른쪽 끝부터 채워 구분선 쪽으로
  자라납니다. 즉 가장 최근 경기가 항상 가운데(구분선) 가까이 옵니다.
- **셀 색상**: RT(핸승=파랑/핸무=초록/무=회색/역=빨강), 핸디기준점(음수=파랑/양수=빨강).
  RT·PICK(적중/보험/미적/관망) 배지는 옅은 배경 틴트 + 진한 글씨의 "칩" 스타일이며,
  다크/라이트 값이 `web/src/index.css`의 `--chip-*` 변수로 갈려 있다.
- **비슷한 구조가 반복되는 데이터는 문장이 아니라 표로.** "홈(정) 핸승(4) 핸무(4) 무(3) 역(3)"
  처럼 항목(핸승/핸무/무/역 등)이 여러 대상(홈/원정 등)에 걸쳐 나열식 문장으로 반복되면
  자릿수가 안 맞아 읽기 힘들다 — 대상을 행, 항목을 열로 둔 작은 표로 바꿔서 라벨 폭을
  맞추고 항목 사이에 구분선을 준다. 기존 `.detail-table` 스타일을 그대로 재사용해 앱 전체
  표 톤과 맞춘다(예: 상세보기 팝업의 종합픽 "시즌전적" 카드, `web/src/components/MatchDetailModal/`
  의 `SeasonRowsTable` 참고).

### 6-1. 간격·크기 눈금

새 CSS를 쓸 때는 아무 숫자나 넣지 말고 **이미 앱에서 많이 쓰는 값**을 고른다. 아래는 실제
사용 빈도를 센 결과다(`web/src` 전체 기준). 눈금에 없는 값을 쓰면 그 부분만 미묘하게 어긋나 보인다.

| 용도 | 값 | 쓰는 곳 |
|---|---|---|
| 아주 붙은 한 덩어리 | `gap: 2px` `4px` | 칩 안의 라벨+숫자, 라벨 위아래 |
| 제목과 부가설명 사이 | `gap: 6px` | 섹션 제목 옆 note |
| **나열 항목 사이 (기본)** | **`gap: 8px`** | 요약 바, 배지 줄, 버튼 줄 |
| 카드·블록 사이 | `gap: 10px` `12px` | 카드 그리드, 폼 행 |
| 큰 영역 구분 | `gap: 20px` `22px` `28px` | 툴바, 통계 묶음 |

- **글자 크기**: `13px` 기본(본문·제목) → `12px` 표 → `11px` 부가설명 → `10px` 아주 조밀한 표
- **모서리**: `6px` `8px` 기본, 표 안 작은 칩은 `4px`, 알약형 배지는 `12px`(또는 `999px`)
- **안쪽 여백**: 칩 `2px 6~8px`, 버튼·입력 `7px 10px`, 카드 `12px 14px`

### 6-2. `A · B · C` 나열 표기

여러 값을 한 줄로 늘어놓을 때는 **가운뎃점(`·`)으로 구분**한다. 기준이 되는 예시는 리그 화면
상단 요약 바다: `등록된 시즌 18 · 경기수 5,229 · 국배 등록 3,441`.

- **값은 `<strong>`으로 밝게**(`var(--text-primary)`), 라벨과 구분자는 흐리게(`var(--text-muted)`)
- 등급·상태처럼 **분류를 나타내는 값은 칩**으로 뺀다(`RtSummaryBar`의 알약형 배지와 같은 톤)
- 컨테이너는 `display: inline-flex` + `gap: 8px`

> ⚠ **구분자를 텍스트 공백(`' · '`)으로만 두면 안 된다.** 브라우저가 연속 공백을 하나로
> 합쳐 버려서 항목마다 간격이 들쭉날쭉해진다. 반드시 flex의 `gap`으로 벌린다.
> (예: 상세보기 배당 제목 옆 `똥1 · 안전 17% · 똥사` — `.detail-ddong` 참고)

---

## 7. 검증

- 프론트 수정 후: `cd web && npx oxlint src/`
- 화면 동작은 개발 서버가 켜진 상태에서 사용자가 브라우저로 직접 확인합니다.
- 백엔드 수정 후: `API_실행.bat`이 켜져 있으면 `--reload`로 자동 반영됩니다.
