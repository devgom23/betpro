# BETPRO React 전환 — 클로드 코드 인수인계 문서

> 이 문서를 클로드 코드 대화 맨 처음에 붙여넣으세요.

---

## 0. 먼저 읽어주세요 (작업 규칙)

- **한국어로만 소통해주세요.** 채팅 답변뿐 아니라 도구 사용 시 표시되는 설명 문구까지 전부 한국어로 부탁합니다. 영어·일본어가 섞이면 안 됩니다.
- 코드 수정 후에는 **검증까지 마친 뒤**, 결과를 프로그래밍을 모르는 사람도 이해할 수 있는 쉬운 말로 설명해주세요.
- 저는 개발자가 아닙니다. 명령어를 알려주실 때는 **어디에 무엇을 입력하는지**까지 알려주세요.
- 클로드 코드의 모델의 어떤걸 사용해야 하는지 시작할때 나한테 알려줘야 되 즉, opus로 해야할것과 그냥 sonnet으롷 해야 하는것을 나한테 알려줘서 프로젝트를 진행하는데 있어 최소의 토큰으로 최대의 결과를 내고 싶어 

---

## 1. 프로젝트 개요

**BETPRO (WEB_BET PRO)** — 축구 핸디캡 베팅 분석 시스템

- 대상 리그: EPL, 라리가, 세리에A, 분데스리가, 에레디비시, 리그1 (유럽 6개)
- 과거 경기 데이터와 배당률을 분석해 26개 지표를 산출하고, 핸디캡 결과를 예측
- 현재 Streamlit(파이썬 웹 프레임워크)으로 만들어져 있고, 내 PC에서 로컬 서버로 돌린 뒤 Cloudflare Tunnel로 외부에 노출하는 구조

**핵심 도메인 용어**
- **RT(경기 결과)**: 1=핸승, 2=핸무, 3=무, 4=역
- **핸승**: 정배(이길 것으로 예상된 팀)가 핸디캡을 커버한 경우
- **플핸(플러스 핸디)**: 핸승이 아닌 나머지 전부 (핸무+무+역). 즉 언더독 쪽 베팅
- **fav(정배배당)**: min(FW, FL). 배당이 낮은 쪽 = 시장이 강하다고 본 쪽

**배당 데이터 컬럼**
- 해외: FW(승) FD(무) FL(패) FH(핸디기준점) FHW(핸디승) FHD(핸디무) FHL(핸디패)
- 국내: KW KD KL KH KHW KHD KHL (동일 구조)
- 경기정보: L(리그) S(시즌) R(라운드) No(경기번호) DT(일자) TM(시간) HT(홈팀) HS(홈득점) AT(원정팀) AS(원정득점) RT(결과)

---

## 2. 왜 React로 옮기려 하는가

Streamlit의 구조적 한계 때문입니다. 실제로 겪은 문제들:

- 위젯(버튼·입력칸) 하나만 건드려도 **스크립트 전체가 처음부터 다시 실행됨** → 체크박스 클릭만 해도 화면 전체 로딩
- 표에서 **2줄 헤더와 체크박스를 동시에 구현 불가** (둘 중 하나만 됨)
- 쿠키 처리가 부실해 로그인 유지·아이디 저장이 불안정
- 안 보이는 탭까지 매번 전부 계산 (리그 6개 = 매번 약 6초)
- 위젯 값을 코드에서 직접 못 바꾸는 제약 → 초기화 버튼이 앱을 죽이는 버그 발생

이런 문제들이 **코드를 잘못 짜서가 아니라 Streamlit 자체의 한계**에서 나오는 것이라 판단했습니다.

**목표 구조**: 분석 로직(파이썬)은 그대로 두고, 화면(UI)만 React로 새로 만들어 API로 통신

---

## 3. 개발 환경 세팅 (처음부터)

### 3-1. 이미 설치되어 있는 것
- Windows PC
- Python 3.12
- VS Code
- Chrome
- **Node.js (설치 완료)**

### 3-2. Node.js 설치 확인
명령 프롬프트(cmd) 또는 VS Code 터미널에서:
```
node -v
npm -v
```
둘 다 버전 숫자가 나오면 정상입니다. 안 나오면 https://nodejs.org 에서 LTS 버전 재설치.

### 3-3. React 프로젝트 생성 (아직 안 함 — 클로드 코드와 함께 진행 예정)
```
npm create vite@latest betpro-web -- --template react
cd betpro-web
npm install
npm run dev
```
- 예전엔 `create-react-app`을 썼지만 요즘은 더 빠른 **Vite**가 표준입니다
- `npm run dev` 하면 로컬 개발 서버가 켜지고 브라우저에서 확인 가능

### 3-4. 백엔드 API 서버 (신규 필요)
지금 파이썬 분석 로직을 React가 쓰려면 API 서버가 필요합니다. **FastAPI** 또는 **Flask** 중 선택 예정.
```
pip install fastapi uvicorn
```

### 3-5. VS Code 확장 프로그램 (선택, 있으면 편함)
- ES7+ React/Redux/React-Native snippets
- Prettier
- Tailwind CSS IntelliSense (Tailwind 쓸 경우)

---

## 4. 현재 소스 구성 (Streamlit 버전, V2.3)

총 5개 파이썬 파일, 약 4,600줄

| 파일 | 줄 수 | 역할 |
|---|---|---|
| `WEB_BET_PRO.py` | 2,450 | 본체 (엔진 + 화면 전체) |
| `betpro_ui.py` | 793 | 상세 경기 정보 팝업, 스코프 바, 마스터 관리 |
| `betpro_paths.py` | 687 | 4-DB 경로 관리, 백업/복구 |
| `betpro_auth.py` | 326 | 로그인 인증, 계정 관리, 자동로그인 토큰 |
| `betpro_migrate.py` | 356 | DB 마이그레이션 도구 |

**현재 의존성** (`requirements.txt`)
```
streamlit>=1.30
pandas>=2.0
numpy>=1.24
openpyxl>=3.1
xlsxwriter>=3.1
streamlit-cookies-manager>=0.2.0
```

### 4-DB 아키텍처 (그대로 유지할 것)
| DB | 경로 | 용도 |
|---|---|---|
| auth.db | 루트 | 계정 정보 (master 재빌드와 무관하게 보존) |
| master.db | data/master/ | 공식 데이터 (관리자만 쓰기, 전 고객 열람) |
| user.db | data/users/{id}/ | 개인 업로드 데이터 (본인만) |
| access_log.db | data/ | 관리자 열람 기록 |

**중요 원칙**: 공식 데이터와 개인 데이터는 **DB 파일 자체가 분리**되어 완전 격리. 통합 지표도 각자의 스코프 안에서만 계산됨.

---

## 5. 반드시 그대로 살려야 할 것 (분석 엔진)

아래 함수들이 **핵심 자산**입니다. React로 옮겨도 이 로직은 파이썬 그대로 재사용합니다.

### 5-1. 핵심 엔진 함수 (`WEB_BET_PRO.py` 내부)
| 함수 | 줄 위치 | 역할 |
|---|---|---|
| `preprocess_data()` | 613 | 엑셀 원본 → 표준 형식 정리 |
| `_prep_db()` | 778 | DB를 numpy 배열로 1회 전처리 (성능 최적화 핵심) |
| `get_samples_fast()` | 808 | 26개 지표별 과거 표본 카운트 |
| `analyze_row()` | 918 | 경기 1건의 전체 지표 산출 |
| `analyze_dataframe()` | 1121 | 배치 처리 (캐시 재사용) |
| `compute_plushandi()` | 1077 | 플핸 예측 산출 (아래 6번 참고) |
| `_prep_domestic_cache()` | 721 | 국내 배당 도플갱어 캐시 |

**절대 원칙**: 이 함수들의 계산 로직은 건드리지 마세요. 성능 최적화(numpy 캐싱)와 정확성이 오랜 검증을 거쳐 확정된 부분입니다.

### 5-2. 26개 지표 체계
코드 규칙: `[T]{F|K}-{결과}[-{기준}]`
- T = 6대리그 통합 / F = 해외배당 / K = 국내배당
- 결과: W(승) L(패) WL(승+패) WDL(승+무+패)
- 기준: HW(핸디정배) HT(홈팀명) AT(원정팀명)

**해외 13개 (지표 1~13)**
```
F-W, F-L, F-WL, F-WDL, F-W-HW, F-W-HT, F-L-AT, F-WL-HT, F-WL-AT,
TF-W, TF-L, TF-WL, TF-WDL
```

**국내 13개 (지표 14~26)**
```
K-W, K-L, K-WL, K-WDL, K-W-HW, K-W-HT, K-L-AT, K-WL-HT, K-WL-AT,
TK-W, TK-L, TK-WL, TK-WDL
```

각 지표는 4칸(핸승/핸무/무/역) 구조로, 현재 경기와 조건이 일치하는 과거 경기가 결과별로 몇 건인지 세어놓은 값입니다. (자기 자신 1건은 제외)

---

## 6. 예측 시스템 (최근 전면 개편, 꼭 읽어주세요)

### 6-1. 옛 예측 엔진은 삭제됨
예전에 `ProgramPredictor18`이라는 복잡한 예측 엔진(리그별 룰북, 베이지안 확률, fav 구간별 등급 등)이 있었으나 **V2.3에서 완전히 삭제**했습니다.

**삭제 이유** (실측 검증 결과):
- fav(정배배당) 구간이 픽/등급을 사실상 전부 결정하고, 26개 지표는 거의 반영 안 됨
- 실험: fav=1.30일 때 지표가 전부 "역"을 가리켜도, 지표가 하나도 없어도 → 무조건 "핸승 S"
- 26개 지표로 4개 결과 맞히기는 어떤 조합으로도 **30~38%** (베이스레이트 31%와 차이 없음)
- 원인: 26개 지표가 전부 "배당이 같은 과거 경기 찾기"라 배당 정보의 재포장에 가까움
  (지표 핸승비율 ↔ fav 상관계수 **-0.881**)

### 6-2. 현재 예측 = 플핸 예측 (V2.2 신설)
4개 결과 맞히기는 포기하고, **2분류(핸승 vs 플핸)**로 좁히니 실제 예측력이 나왔습니다.

**산출 컬럼 5개** (`compute_plushandi()` 함수)
| 컬럼 | 내용 |
|---|---|
| `PH_F` | 해외 13개 지표 표본의 비핸승 비율(%) |
| `PH_K` | 국내 13개 지표 표본의 비핸승 비율(%) |
| `PH_PICK` | 플핸(역) / 플핸(무) / 플핸(핸무) / 핸승 / —(관망) |
| `PH_HIT` | 실측 적중률(%) — 등급(S/A/B) 대신 실제 수치 표시 |
| `PH_DOM` | 최다결과가 비핸승 표본에서 차지하는 비율(%) |

**실측 보정표** (EPL 1,135경기 백테스트 근거, 코드 내 `PH_TABLE` 상수)
- 전체 베이스: 플핸 69.0%
- 비핸승 중 **'핸무'가 최다일 때 62.2%로 급락** (역 73.3% / 무 73.4%) → PICK에 괄호 표기 + 주황색 경고
- 해)플핸 85%+ & 역최다 → 85.7% (최고 구간)
- 해)플핸 40~50% 구간은 오히려 핸승이 60.0% → PICK을 '핸승'으로
- 50~60% 구간은 베이스 수준이라 정보 없음 → 관망(—)

**검증 결과** (760경기): 화면 표시치 대비 실제 오차 **최대 1.4%p** — 매우 정확

### 6-3. 계산 시점 원칙 (중요)
- 26개 지표와 플핸 예측은 **엑셀 업로드([저장]) 시점에 단 한 번만** 계산되어 DB에 저장
- 화면에서 시즌/라운드/배당 조건을 바꾸거나 조회해도 **저장된 값을 불러오기만** 함. 절대 재계산 안 함
- 예외: 통합DB 탭의 재계산 버튼 2종
  - `🔄 통합 및 예측 분석`: RT 없는 예정 경기만 재계산 (과거 경기 무수정)
  - `🔧 전체 재계산`: 과거 포함 전체 재계산 (초기 세팅용)

---

## 7. 현재 화면 구성 (React로 다시 만들 대상)

### 7-1. 로그인 화면
- 아이디/비밀번호 입력
- "로그인 유지" 체크박스 (계정 만료일까지 자동 로그인, HMAC 서명 토큰 쿠키 저장)
- "아이디 저장" 체크박스 ⚠️ **현재 작동 안 함 — React에서 제대로 구현 필요**
- 비밀번호는 PBKDF2-HMAC-SHA256 해시로 저장 (평문 미저장)

### 7-2. 상단 바
- 접속 계정 표시, 이용 기간(D-day), 로그아웃 버튼
- 스코프 선택: `📊 공식 데이터` / `👤 내 데이터` (라디오)

### 7-3. 탭 구성
`EPL · 라리가 · 세리에A · 분데스 · 에레디 · 리그1 · 📈통합DB · 🆚상대전적` (+관리자: `🛠마스터관리 · 👑계정관리`)

### 7-4. 리그 탭 (메인 화면)
1. **조회 필터** (폼으로 묶여 있음 — 조회 버튼 눌러야 반영)
   - 시즌 / 라운드 선택 (기본값 = 최근 시즌 · 최근 라운드 자동 선택)
   - 국내 배당 입력 3칸 (KW/KD/KL)
   - 국내 플핸 배당 3칸 (KHW/KHD/KHL)
   - 해외 배당 3칸 (FW/FD/FL)
   - `↺ 초기화` / `🔍 조회` 버튼
2. **요약 바**: 등록 경기수, 핸승/핸무/무/역 건수와 비율
3. **★ 대형 분석표** (가장 중요)
   - 2줄 헤더: 위=그룹명, 아래=세부항목
   - 컬럼 순서: 일반정보 → 경기정보 → 국내배당 → 해외배당 → **플핸 예측 5칸** → 26개 지표(각 4칸)
   - 셀 색상: RT(핸승=파랑/핸무=하늘/무=회색/역=빨강), 플핸 예측(픽 종류별), 핸디기준점(음수=파랑/양수=빨강)
   - 행 선택 가능 (체크박스)
4. **상세 경기 정보 팝업** (행 선택 후 버튼 클릭 시)
   - 좌: 배당표 / 예측 / 지표별 표본
   - 우: 상대전적 (요약 + 경기 목록, 최신순)
   - 엑셀 다운로드 2종 (상대전적만 / 전체)
5. **엑셀 다운로드** (화면과 동일한 2줄 헤더 구조)
6. **업로드 영역** (쓰기 권한 있을 때만)

### 7-5. 통합DB 탭
- 리그별 업로드 현황 대시보드
- 재계산 버튼 2종 (위 6-3 참고)
- 6개 리그 합산 데이터 조회

### 7-6. 상대전적 탭
- 두 팀 선택 → 맞대결 기록 (양날개 W/D/L 집계)

### 7-7. 관리자 탭
- 마스터 관리: 공식 DB 백업/복구
- 계정 관리: 계정 목록/추가/삭제/기간연장/비밀번호 변경, 고객 데이터 열람(읽기전용+로그 기록)

---

## 8. React 전환 시 고려할 점

### 8-1. 구조 제안
```
[React 프론트엔드]  ←── HTTP API ──→  [Python 백엔드]
  - 화면 전부                            - 기존 분석 엔진 그대로
  - 표, 필터, 팝업                       - FastAPI/Flask로 API 노출
  - 로그인 UI                            - SQLite DB 접근
```

### 8-2. 우선순위 제안
1. Python 쪽에 API 서버 만들기 (기존 엔진 함수를 API로 감싸기)
2. React 프로젝트 초기 세팅 + 로그인 화면
3. **대형 분석표** (가장 어렵고 중요) — 2줄 헤더 + 셀 색상 + 행 선택
4. 조회 필터
5. 상세 경기 정보 팝업
6. 나머지 탭들
7. 관리자 화면

### 8-3. 디자인
피그마로 작업한 디자인이 있습니다. (별도 전달 예정)

### 8-4. 특히 신경 써야 할 부분
- **대형 표 성능**: 컬럼이 130개 이상, 행은 수백~수천 개. 가상 스크롤 필요할 수 있음
- **2줄 헤더**: 그룹명 셀은 여러 컬럼에 걸쳐 병합(colspan)
- **셀 색상**: 값에 따라 배경색이 달라지는 규칙이 많음
- **로그인 유지/아이디 저장**: Streamlit에서 제대로 안 되던 부분. React에선 확실히 구현

---

## 9. 남은 이슈 / 미해결 사항

1. **"아이디 저장" 기능 작동 안 함** — Streamlit 쿠키 라이브러리 문제로 추정. React 전환 시 자연히 해결될 것으로 기대
2. **플핸 예측 보정표가 EPL 기준** — 다른 리그는 수치가 다를 수 있어, 리그별 보정 필요
3. **상업화 준비** (React 전환과 별개)
   - 기본 관리자 비밀번호 변경 필요
   - 한국 유료 스포츠 예측 서비스 관련 법적 검토 필요 (변호사 확인 필수)
   - Cloudflare Tunnel 고정 도메인
   - data 폴더 백업 체계

---

## 10. 참고: 지금까지의 버전 이력

- **V1.0**: Streamlit 웹 서버판 전환, 4-DB 구조, 로그인 게이트
- **V2.0**: 26개 지표 체계 전면 개정, 2줄 헤더 복원, 예측 재계산 제거, 재계산 버튼 2종 신설
- **V2.1**: 조회 필터 폼 처리, 탭 지연 렌더링(6배 속도 개선), 로그인 화면 개선
- **V2.2**: 플핸 예측 5종 신설 (26지표 기반, 실측 적중률 표시)
- **V2.3**: 옛 예측 엔진(ProgramPredictor18) 및 예측저장/자동채점 완전 삭제 ← **현재**

---

## 11. 첫 요청

위 내용 이해하셨으면, 우선 **전체 구조 설계**부터 같이 잡고 싶습니다.

1. Python API 서버는 FastAPI와 Flask 중 뭐가 나을지
2. React 프로젝트 폴더 구조를 어떻게 나눌지
3. 기존 Streamlit 코드에서 어떤 부분을 어떻게 떼어낼지

이 세 가지에 대한 제안을 먼저 듣고 싶습니다. 코드 작성은 방향이 정해진 다음에 시작하면 좋겠습니다.
